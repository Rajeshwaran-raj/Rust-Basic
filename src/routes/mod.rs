pub mod users; pub mod products; pub mod orders; use rocket::serde::json::Json; use serde::Serialize; #[derive(Serialize)] pub struct Health{pub status:&'static str} #[get("/healthz")] pub fn healthz()->Json<Health>{Json(Health{status:"ok"})}
