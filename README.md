# Rocket + SQLx (MySQL) starter. Endpoints: /healthz, /api/users, /api/products, /api/orders.
# 🚀 Rocket Demo API

A simple RESTful API built with **Rust** and **Rocket** framework that provides endpoints for managing users, products, and orders.

---

## 📦 Quick Start
cargo run


### 🗄️ 1. Create Database

```sql
CREATE DATABASE rocket_demo CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
